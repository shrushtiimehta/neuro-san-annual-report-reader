# Neuro SAN
neuro-san==0.6.41
nsflow==0.6.11

# To extract text from PDF documents (used by annual_report_reader)
pypdf>=6.1.3

# To use a .env file for environment variables
python-dotenv==1.0.1

# Neuro-SAN now requires explicit installation of langchain LLM providers' libs
langchain-anthropic>=1.0.0,<2.0

# For rich logging
rich>=14.2.0

# For HTTP requests (used by annual_report_reader tools)
requests>=2.32.0
